# Ensure you have setup Docker and created a NodeJS container
#    See dockerReadMe.md
# docker build -f DockerfileNodeNano -t angular2template:nodenano .
# docker run -it -p 8080:3000 angular2template:nodenano
# docker inspect <containerId>
# Test on http://<containerIP>:3000
FROM node:nano
RUN mkdir build
COPY build /build
WORKDIR /build
# RUN npm install
CMD [ "node", "server.js" ]